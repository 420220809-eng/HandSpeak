[phases.setup]
nixPkgs = ["python311", "gcc", "glib", "libGL"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements_api.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
